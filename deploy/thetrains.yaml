apiVersion: v1
kind: Service
metadata:
  name: thetrains
  namespace: thetrains
  labels:
    app: thetrains
spec:
  selector:
    app: thetrains
  ports:
    - name: http
      port: 80
      targetPort: 8000

---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: thetrains
  namespace: thetrains
  annotations:
    kubernetes.io/ingress.class: "nginx"
    cert-manager.io/cluster-issuer: "letsencrypt-prod" # Can comment this out for no certificates
spec:
  tls:
    - hosts:
        - <DOMAIN>
      secretName: thetrains-tls
  rules:
    - host: <DOMAIN>
      http:
        paths:
          - path: /
            backend:
              serviceName: thetrains
              servicePort: 80

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: thetrains
  namespace: thetrains
spec:
  replicas: 1
  selector:
    matchLabels:
      app: thetrains
  template:
    metadata:
      labels:
        app: thetrains
    spec:
      containers:
        - name: thetrains
          image: registry.gitlab.com/joshtingey/the-trains/thetrains:latest
          imagePullPolicy: "Always"
          ports:
            - containerPort: 8000
